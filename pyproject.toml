[project]
name = "book-writer-crewai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "crewai[tools]>=0.140.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.2.0",
]
